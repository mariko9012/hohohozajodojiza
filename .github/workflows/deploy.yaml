on: push
name: 🚀 Ddeploy to jodojiza.co
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - uses: actions/setup-node@v3
      with:
        node-version: 16
    - run: npm ci
    - run: npm run generate

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v3.1.2
      with:
        ftp-server: joudojiza.sakura.ne.jp
        ftp-username: joudojiza
        ftp-password: ${{ secrets.PASSWORD }}
        local-dir: ./dist/
        entryPoint: ~www/
